import RestaurantContextProvider, {
  RestaurantContext,
} from "./restaurant.context";
import FavoritesContextProvider, {
  FavoritesContext,
} from "./favorites.context";

export {
  RestaurantContext,
  RestaurantContextProvider,
  FavoritesContext,
  FavoritesContextProvider,
};
